<h1>This is the testing category, hopefully it is not visible in production</h1>

<% @questions.each do |question| %>
  <div class="question" id="<%= question.short_name %>">
    <%= question.text %>
  </div>
<% end %>
